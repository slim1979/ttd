<p id="notice"><%= notice %></p>

<p>
  <strong>Поезд номер:</strong>
  <%= @train.number %>

  <% if @train.vans.empty? %>
    <br />
    К поезду не присоединены вагоны
  <% else %>
    <br />
    <%= form_for(@train) do |form| %>
      <div><%= form.radio_button(:sort_by, "increase") %>С головы
                      <%= form.radio_button(:sort_by, "decrease") %>С хвоста
                      <%= form.submit 'Сортировать' %></div>
      <br />
    <%= render @train.vans, train: @train %>
    <% end %>
  <% end %>
</p>

<p>
  <strong>Текущий маршрут:</strong>
  <% if @train.route %>
    <%= @train.route.title %>
  <% else %>
    Нет назначенного маршрута
  <% end %>
  <br />
  <strong>Текущая станция:</strong>
  <% if @train.current_station %>
    <%= @train.current_station.title %>
  <% else %>
    Не указано
  <% end %>
</p>

<%= link_to 'Редактировать', edit_train_path(@train) %> |
<%= link_to 'Назад', trains_path %>
